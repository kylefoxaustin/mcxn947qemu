/*
 * Auto-generated from the MCXN947 CMSIS header: { base, register-window
 * size, name } for peripherals covered by the generic permissive stub
 * (mcxn_stub).  Functionally-modelled blocks (SYSCON, SPC, SCG, GPIO,
 * PORT, LPUART4 console) are intentionally excluded.  Upgrade entries to
 * real device models over time; drop them here as that happens.
 *
 * SPDX-License-Identifier: GPL-2.0-or-later
 */
